#!/bin/bash

# This file contains 'gem install' commands which are to be integrated into the
# Dockerfile being built. Any other lines *should* be ignored; however, please
# don't add other commands you expect to have executed by said Dockerfile here.

gem install --no-document hanami:1.3.1 hanami-model:1.3.2 hanami-webconsole dotenv
